'use strict';

// todo: make calls to API, manipulate API data, present results

function submitClicked() {
  // this function will take user input from the search bar
  // erase previous results if any exist
  // call functions to reach out to APIs for data
}

$(submitClicked);